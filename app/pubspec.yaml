name: app
description: "Daktar Lamara App"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  cloud_firestore: ^5.4.0
  connectivity_plus: ^6.0.5
  cupertino_icons: ^1.0.8
  firebase_analytics: ^11.3.0
  firebase_auth: ^5.2.0
  firebase_core: ^3.4.0
  firebase_crashlytics: ^4.1.0
  firebase_messaging: ^15.1.0
  firebase_storage: ^12.2.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  flutter_native_splash: ^2.4.1
  flutter_riverpod: ^2.5.1
  geolocator: ^12.0.0
  go_router: ^14.2.7

dev_dependencies:
  custom_lint: ^0.6.5
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  riverpod_lint: ^2.3.13

flutter:
  uses-material-design: true
